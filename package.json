{
  "name": "hammad-media-website",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "node build.js",
    "test": "node --test test/*.test.js tests/intake*.test.js",
    "test:intake:postgres": "node --test tests/intake-postgres.test.js",
    "dev": "node build.js && node dev.js"
  }
}
